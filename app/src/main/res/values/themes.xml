<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.BizCall" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.BizCall" parent="Base.Theme.BizCall" />

    <!-- ★ 스플래시 테마 -->
    <style name="Theme.BizCall.Splash" parent="Theme.BizCall">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>