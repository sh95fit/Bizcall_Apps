<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#F8F9FA">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BizCall 업무폰 등록"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#1A1A2E"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="관리자에게 받은 QR 또는 토큰을 입력하세요"
        android:textSize="14sp"
        android:textColor="#6C757D"
        android:gravity="center"
        android:layout_marginBottom="32dp"/>

    <!-- QR 스캔 버튼 -->
    <Button
        android:id="@+id/btnScanQr"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="QR 코드 스캔"
        android:textSize="16sp"
        android:backgroundTint="#4CAF50"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="또는 토큰 직접 입력"
        android:textSize="12sp"
        android:textColor="#6C757D"
        android:layout_marginBottom="16dp"/>

    <!-- 토큰 직접 입력 -->
    <EditText
        android:id="@+id/etToken"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:hint="등록 토큰 입력"
        android:inputType="text"
        android:background="@android:drawable/edit_text"
        android:padding="12dp"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/btnRegister"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="토큰으로 등록"
        android:textSize="16sp"
        android:backgroundTint="#2196F3"
        android:layout_marginBottom="24dp"/>

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="14sp"
        android:gravity="center"
        android:textColor="#6C757D"/>

    <!-- 수동 업로드 버튼 (등록 완료 후에만 표시) -->
    <Button
        android:id="@+id/btnManualUpload"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="이전 녹음 파일 업로드"
        android:visibility="gone" />

    <!-- 실패 파일 개수 표시 -->
    <TextView
        android:id="@+id/tvFailedCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="업로드 실패 없음"
        android:textAlignment="center"
        android:visibility="gone" />

    <!-- 실패 파일 재시도 버튼 -->
    <Button
        android:id="@+id/btnRetryFailed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="실패 파일 재시도"
        android:enabled="false"
        android:visibility="gone" />

</LinearLayout>
